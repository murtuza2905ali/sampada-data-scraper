services:
  - type: web
    name: sampada-data-scraper
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn myproject.wsgi:application --timeout 120 --workers 2"
